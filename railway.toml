[deploy]
  startCommand = "HOSTNAME=0.0.0.0 node web/server-standalone/server.js"
  healthcheckPath = "/api/health"
  healthcheckTimeout = 300
  restartPolicyType = "on_failure"
  restartPolicyMaxRetries = 3
